declare function getDistrict(ubigeo: string): string | null;

declare function getProvince(ubigeo: string): string | null;

declare function getDepartment(ubigeo: string): string | null;

declare function getDepartments(): {
    code: string;
    name: string;
}[];

declare function getProvinces(departmentCode: string): {
    code: string;
    name: string;
}[];

declare function getDistricts(provinceCode: string): {
    code: string;
    name: string;
}[];

interface UbigeoData {
    ubigeo: string;
    district: string;
    province: string;
    department: string;
    population: string;
    area: string;
    lat: string;
    lng: string;
}

declare function getUbigeoData(ubigeo: string): UbigeoData | null;

declare function parseUbigeo(ubigeo: string): {
    departmentCode: string;
    provinceCode: string;
    districtCode: string;
} | null;

declare function formatUbigeo(ubigeo: string, format?: 'full' | 'short'): string;

declare function validateUbigeo(code: string): boolean;
declare function isValidDepartment(code: string): boolean;
declare function isValidProvince(code: string): boolean;
declare function isValidDistrict(code: string): boolean;

export { formatUbigeo, getDepartment, getDepartments, getDistrict, getDistricts, getProvince, getProvinces, getUbigeoData, isValidDepartment, isValidDistrict, isValidProvince, parseUbigeo, validateUbigeo };
